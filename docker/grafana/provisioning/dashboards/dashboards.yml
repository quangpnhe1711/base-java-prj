apiVersion: 1

providers:
  - name: default
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      # Drop dashboard JSON files here; they are picked up automatically.
      path: /etc/grafana/provisioning/dashboards
